apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: java-app
    replica: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80