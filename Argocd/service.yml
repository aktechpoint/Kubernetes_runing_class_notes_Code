apiVersion: v1
kind: Service
metadata:
  name: student-project-service
  labels:
    app: django-app
spec:
  type: LoadBalancer
  selector:
    app: django-app
  ports:
    -port: 80
    targetPort: 8000