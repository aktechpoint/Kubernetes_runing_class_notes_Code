apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment-blue-v1
  labels:
    app: java-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-app
      replica: blue
  template:
    metadata:
      labels:
        app: java-app
        replica: blue
    spec:
      securityContext:
        fsGroup: 1000
      containers:
      - name: blue-container
        image: nginx:latest
        ports:
        - containerPort: 80
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
