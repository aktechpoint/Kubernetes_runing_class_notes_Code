apiVersion: v1
kind: Service
metadata:
  name: kundan-service
  labels:
    app: kundan-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: kundan-app